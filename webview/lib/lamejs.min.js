// lamejs placeholder - Audio is sent as WebM format which Whisper supports directly
// If MP3 conversion is needed, replace this file with the actual lamejs library from:
// https://github.com/zhuker/lamejs/blob/master/lame.min.js
window.lamejs = {
  Mp3Encoder: function() {
    console.warn('lamejs placeholder: MP3 encoding not available. Using WebM format instead.');
    return {
      encodeBuffer: function() { return []; },
      flush: function() { return []; }
    };
  }
};
